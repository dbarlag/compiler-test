name: DS Lint
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ds-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node .github/scripts/ds-lint.mjs
